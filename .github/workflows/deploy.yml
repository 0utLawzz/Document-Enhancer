name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: docbright-web/package-lock.json

      - name: Install dependencies
        # npm install (not ci) so package.json deps like pdfjs-dist install
        # even if package-lock.json is slightly out of date
        run: npm install
        working-directory: docbright-web

      - name: Build project
        run: npm run build
        working-directory: docbright-web

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: docbright-web/dist
          branch: gh-pages
